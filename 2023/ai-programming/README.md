# 2023 PNU AI Programming

AI 프로그래밍 과목의 실습 파일을 보관한 레포지토리입니다.

## Week 01 – Python 기초 (타입, 변수, 문자열)
- 숫자 · 문자열 타입, 변수 선언 규칙, 산술/비교/논리 연산자, 형 변환
- 문자열 결합, 인덱싱/슬라이싱, 포매팅, 이스케이프 시퀀스, 입력 처리
- **Lab**: 변수 네이밍 교정, 복합 산술식 계산, 타입 변환, 비교/논리 연산으로 조건 평가, 문자열 메서드·반복을 이용한 텍스트 조작 문제 풀이.

## Week 02 – 연산자와 제어문
- 산술·비트·비교·논리·멤버십·할당 연산자와 우선순위
- 표현식과 문장의 차이, if/elif/else, while/for, 중첩 루프, 파일 순회, `pass/break/continue` 사용법
- **Lab**: 짝/홀 판별, 세 수 중 최댓값, 공배수 여부, 피타고라스 트리플 판별, BMI 계산, 애너그램 확인, 배수 합산, 구구단, 소수 판별, 문자열 뒤집기+모음 수 세기, 패턴 출력, 중복 탐색, 파스칼 삼각형, DNA 패턴 매칭 등 제어문 연습.

## Week 03 – 시퀀스 자료구조
- 리스트/튜플 특징, 얕은·깊은 복사, 시퀀스 공통 연산(멤버십, 인덱싱, 슬라이싱)
- 문자열 메서드, 참조 동작(같은 객체 공유 주의)
- **Lab**: 리스트 짝수 합·정렬, 튜플 언패킹과 결합, 리스트 컴프리헨션, 필터링+역순+합산, 문자열 조작, 팰린드롬 판별, 투썸, 길이 기반 정렬·딕셔너리 매핑 등 시퀀스 응용 문제 풀이

## Week 04 – 함수, 스코프, 모듈
- 함수 정의/호출, docstring, `return` 동작, 스코프(지역/전역/상수), 정렬의 `key`·`reverse`, `sorted`와 `sort`의 차이
- 모듈 가져오기(여러 형태)와 사용자 정의 모듈 구성, 람다 표현식, 톱다운 설계 기법
- **Lab**: 조건/반복 로직을 함수화(`is_even_or_odd`, `largest_of_three`, `calculate_bmi` 등)하고, 반복문 기반 유틸리티, 간단한 쇼핑리스트 클래스 등을 구현

## Week 05-1 – 파일, CSV, 딕셔너리/셋
- 텍스트 파일 읽기/쓰기, CSV 파싱, 리스트·딕셔너리로 변환, 빈도표/중첩 딕셔너리, 튜플 키 딕셔너리, 집합 연산 등
- **Lab**: 텍스트의 고유 단어 수 집계, 단어 빈도 계산, CSV → 리스트/딕셔너리 로드, 단어 치환 후 새 파일 쓰기.

## Week 05-2 – 예외 처리, 난수, 재귀
- Colab 환경 준비, `mypy` 설치, 사용자 입력 예외 처리, 파일 입출력 예외 대응, 사용자 정의 예외 메시지 패턴
- `random` 모듈로 난수 추출, 재귀 기본/응용 예제

## Week 05-3 – Pandas 데이터 분석
- CSV 로드, 데이터프레임 생성/내보내기, 행·열 추가/삭제, 인덱싱/슬라이싱, 시리즈 연산, 집계, 결측치 처리 전략
- **Lab**: UN 데이터 필터/정렬, 평균/집계, 대륙별 그룹 연산, 상위 N국가 추출, 타이타닉 데이터 결측치 처리 및 요약 통계 계산.

## Week 06 – 클래스와 객체지향
- 사용자 정의 클래스, 속성 캡슐화, 리스트로 객체 관리, 상속/오버라이드, 서브클래스 속성 확장, 클래스/정적 메서드 데코레이터
- **Lab**: `ShoppingCart`, `TimeSeriesData`, `SimpleLinearRegression` 등 클래스를 설계해 상태 관리, 통계 계산, 단순 회귀 예측 메서드

## Week 08 – NumPy & Matplotlib
- 배열 생성, dtype/shape 확인과 재구조화, 인덱싱/슬라이싱, 브로드캐스팅, 수학/통계/선형대수 함수, 난수/정규분포, 행렬 연산
- Matplotlib 빠른 시작: Figure/Axes/Axis/Artist 개념, 마커·라인·컬러 포맷, `pyplot` vs OO 방식, 보조 함수 활용법
- **Lab (NumPy)**: 이미지 필터 브로드캐스팅, 행렬 조건 필터, 난수 통계 비교, 정규분포 구간 비율, 행렬 곱/아다마르 곱, 시그모이드 적용, 부분 행렬 재배열, MSE 계산 등 10개 과제.
- **Lab (Matplotlib)**: 선/산점/파이/히스토/막대/시계열/박스플롯을 만들어 데이터 시각화 패턴을 연습

## Week 09 – 알고리즘 기초
- 직사각형·정사각형 클래스 설계
- 인터벌 스케줄링용 그리디 알고리즘
- 피보나치/지수 함수의 재귀 vs 반복 구현
- 소인수분해
- 그래프 BFS와 미로 탐색 구현

## Week 10 – Hill Climbing 실습
- `HillClimber` 베이스를 확장하여 First-Choice, Steepest-Ascent 변형을 구현
- 이웃 생성/선택 전략을 비교
- 볼록 함수/테스트 함수(Ackley 등)를 대상으로 탐색 성능을 실험
- Random Restart 전략과 파라미터화된 모드(최대화/최소화) 비교, 연속/이산 문제에서의 작동 방식을 기록

## Week 11 – 고급 탐색 기법
- Hill Climber 버전 히스토리를 추적하며 `num_neighbor`, `num_restarts`, `best_score_idx` 등 확장 포인트를 문서화
- Gradient Descent 기반 Hill Climber, Random Restart, Simulated Annealing 변형을 설계하고 수치 미분/냉각 스케줄/수용 확률 개념을 정리
- Ackley/Griewank/Convex 함수 평가를 통해 탐색 전략 간 탐험-활용 트레이드오프를 비교
